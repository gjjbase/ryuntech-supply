server:
  port: 8761

spring:
  application:
    name: ryun-supply-eureka

eureka:
  instance:
    hostname: ryun-supply-eureka
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
