server:
  port: 8888

spring:
  application:
    name: ryun-supply-config
  main:
    allow-bean-definition-overriding: true

  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://ryun-supply-eureka:8761/eureka/
