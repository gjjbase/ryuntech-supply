server:
  port: 4100

spring:
  application:
    name: ryun-supply-admin-biz

  profiles:
    active: dev
  cloud:
    config:
      name: ${spring.application.name}
      profile: ${spring.profiles.active}

security:
  oauth2:
    client:
      client-id: client
      client-secret: secret
      access-token-uri: http://ryun-supply-auth:4000/oauth/token
      user-authorization-uri: http://ryun-supply-auth:4000/oauth/authorize
    resource:
      token-info-uri: http://ryun-supply-auth:4000/oauth/check_token

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://ryun-supply-eureka:8761/eureka/

# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'

#mybatis配置
mybatis:
  mapper-locations: classpath:/mapper/*Mapper.xml
  type-aliases-package: com.ryuntech.admin.api.entity


mybatis-plus:
    mapper-locations: classpath:/mapper/*Mapper.xml

#mybaatis分页插件pagehelper设置
pagehelper:
  helperDialect: mysql
  reasonable: true
  support-methods-arguments: true

# 打印sql
logging:
  level:
    com.ryuntech.admin.biz.mapper: DEBUG
